{# <?php ?> #}

<form id="addTimesForm">
    <div class="row">
        <div class="col-xs-12">
            <input type="hidden" value="{{Data.id_office}}" name="time[id_office]" id="id_office"/>
            {# 
            <select class="form-control" name="time[id_office]" id="id_office">
                <option value="*">Todos</option>
                {% for item in Data.list_offices.office %}                    
                    <option value="{{item.id}}" {% if Data.id_office == item.id %}selected{% endif %} >{{item.name}}</option>                    
                {% endfor %}
            </select> 
            #}
        </div>
    </div>
    <br/>	
    <label class="btn"> <input type="checkbox" id="timeSlots"/> Todos </label>
    <div id="listTimeSlots">
        <table class="table table-striped table-bordered table-hover">
            {% set used = 0 %}
            {% for key, item in Data.office %}
                <tr>
                    <td>
                        <input type="checkbox" value="{{item.id}}" name="time[slot][]" {% if item.id_office %}checked{% endif %}/> {{item.time|date("H:i")}}
                    </td>
                </tr>
                {% set used = used + ( (item.used > 0 ) ? 1 : 0 ) %}
            {% endfor %}				
        </table>|
    </div>
    <div class="row">
        <div class="col-xs-9" id="msgAddTimeSlots">
        </div>
        <div class="col-xs-3 text-right">
            <button id="addTimes" type="button" class="btn" {% if used > 0 %}disabled{% endif %}>Agregar</button>
        </div>	
    </div>
</form>
