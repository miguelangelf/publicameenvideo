{# <?php ?> #}

<div class="container">
	{% for item in Data.offices %}
		<div class="row">	
			<div class="col-xs-4">	
				<span class="label label-info">{{item.name}}</span>
			</div>
		</div>
	{% endfor %}
</div>