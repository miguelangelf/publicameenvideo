{# <?php ?> #}
<table class="table table-striped table-bordered table-hover">	
	{% for key, item in Data.holiday %}
	<tr>
		<td class="holiday-datail" office-id="{{item.id_office}}" holiday="{{item.date}}">
		{{item.date|date("d/m/Y")}}
		</td>
	</tr>
	{% endfor %}				
</table>